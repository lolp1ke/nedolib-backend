// use nedolib_backend::AppState;

#[derive(Debug)]
struct User {
	username: &'static str,
	password: &'static str,
	email: &'static str,
}

// async fn create(user: User, app_state: AppState) {
// println!("{:?}", user);
// }
